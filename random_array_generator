import numpy as np
M = np.random.randint(0, 10, (20, 20)) #this numbers used and dimensions of arrays (0, 10, (10, 10)) = numbers 1 through 10 and 10x10 dimensions
print(M)
print(M.sum(axis=0))
print(M.sum(axis=1))
